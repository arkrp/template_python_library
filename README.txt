A template for Python Library projects.

Hannah Nelson

creates python libraries with an example dependency (numpy) and an example command (example_command). To install a homade library in python use pip using pip git+<git address> or pip <the directory this readme is in>
